console.log('Hello world')

// Para executar o programa devemos entrar na pasta do index.js pelo terminal e digitar 'node index.js'

console.log(document, window) // No node não temos os elementos document e window do DOM, mas temos um ambiente javascript que funcionará quase que da mesma forma.